spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

ajou:
  swagger:
    server-url: http://localhost:8080

jwt:
  cookie-secure: false
