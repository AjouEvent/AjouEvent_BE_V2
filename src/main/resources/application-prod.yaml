spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

ajou:
  swagger:
    server-url: ${SWAGGER_SERVER_URL}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:https://dev.ajouevent.com}

jwt:
  cookie-secure: true
